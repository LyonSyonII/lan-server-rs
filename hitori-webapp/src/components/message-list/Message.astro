---
import type { Message as MessageProps } from './MessageListElement'
import { parseMarkdown } from './MessageElement';

type Props = MessageProps;

const { date, text } = Astro.props;

// const html = await parseMarkdown(text);
---
<x-message>
  <p>{date.toLocaleString()}</p>
  <div />
</x-message>

<style>
  div > :global(:first-child) {
    margin-top: 0;
  }
  p {
    color: #949ba4;
    font-size: small;
    margin: 0;
  }
</style>

<script>
  import { MessageElement } from "./MessageElement";
  
  customElements.define("x-message", MessageElement);
</script>